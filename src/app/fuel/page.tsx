import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Fuel Page",
  description: "Fuel Page",
};

const FuelPage = () => {
  return <h1>FuelPage</h1>;
};

export default FuelPage;
